# SRT Management Portal — Frontend

React + Vite + Tailwind SPA. See the repository root [`README.md`](../README.md)
and [`docs/`](../docs) for full setup and project documentation.

```bash
cp .env.example .env   # set VITE_API_URL
npm install
npm run dev
```
